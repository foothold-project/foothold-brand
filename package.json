{
  "name": "@foothold-project/brand",
  "version": "1.1.0",
  "private": true,
  "description": "Canonical FOOTHOLD brand tokens, SVG assets, and local Figma synchronization tooling.",
  "type": "module",
  "scripts": {
    "generate": "node scripts/update-compact-spacing.mjs && node scripts/generate-draft-assets.mjs && node scripts/generate-raster-assets.mjs && node scripts/generate-approved-osmu-assets.mjs && node scripts/generate-plugin-data.mjs",
    "generate:check": "node scripts/update-compact-spacing.mjs --check && node scripts/generate-draft-assets.mjs --check && node scripts/generate-raster-assets.mjs --check && node scripts/generate-approved-osmu-assets.mjs --check && node scripts/generate-plugin-data.mjs --check",
    "typecheck": "tsc --project figma/plugin/tsconfig.json",
    "test": "node scripts/verify-repository.mjs && npm run generate:check && npm run typecheck"
  },
  "engines": {
    "node": ">=20.9.0"
  },
  "license": "SEE LICENSE IN LICENSE.md",
  "devDependencies": {
    "@figma/plugin-typings": "1.133.0",
    "@resvg/resvg-js": "2.6.2",
    "sharp": "0.35.3",
    "typescript": "7.0.2"
  }
}
